import java.util.*;

public class midsem {

	public static void main(String[] args) {
		 
		Stack<Integer> st = new Stack<>();
		Queue<Integer> q = new LinkedList<>();
		LinkedList<Integer> ll = new LinkedList<>();
		ArrayList<Integer> arr = new ArrayList<>();
		Set<Integer> s = new HashSet<>();
		HashMap<Integer, Integer> map = new HashMap<>();
		ArrayDeque<Integer> adq = new ArrayDeque<>();
		PriorityQueue<Integer> pq = new PriorityQueue<>(Collections.reverseOrder());
		stack(st);
		queue(q);
		linkedlist(ll);
		arraylist(arr);
		set(s);
		hashmap(map);
		priorityqueue(pq);
		arraydeque(adq);
	}

	private static void arraydeque(ArrayDeque<Integer> adq) {
		
		
	}

	private static void priorityqueue(PriorityQueue<Integer> pq) {
		
		
	}

	private static void hashmap(HashMap<Integer, Integer> map) {
		
		
	}

	private static void set(Set<Integer> s) {
	
		
	}

	private static void arraylist(ArrayList<Integer> arr) {
		 
		
	}

	private static void linkedlist(LinkedList<Integer> ll) {
		 
		
	}

	private static void queue(Queue<Integer> q) {
	
		
	}

	private static void stack(Stack<Integer> st) {
		

	}

}
